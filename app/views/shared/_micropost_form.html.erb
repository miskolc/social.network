<%= form_for(@micropost) do |f| %>
    <%= render 'shared/error_messages', object: @micropost %>

    <div class="field">
        <%= f.text_area :content, placeholder: "Compose new status update..." %>
    </div>
    <%= f.submit "Update Status", class: "btn btn-large btn-primary" %>
<% end %>